name: my_hevo_dbt
profile: my_hevo_dbt

config-version: 2

models:
  my_hevo_dbt:
    +materialized: view

    staging:
      +materialized: view
      +schema: staging

    marts:
      +materialized: table
      +schema: marts